# NYC Yellow Taxi 속도 분석 — 의존성
#
# 하한만 고정한다. 정확히 핀으로 박으면 팀원의 Python 버전에서 설치가 깨질 수 있고,
# 상한 없이 두면 미래의 파괴적 변경에 노출된다.
#
# 검증 환경 (report.md 의 '실행 환경' 표에 실행 시점 버전이 자동 기록된다)
#   Python 3.12.3 / pandas 3.0.2 / polars 1.43.2 / pyarrow 25.0.0 / numpy 2.4.4

# --- 데이터 처리 ---
pandas>=2.0,<4.0
polars>=1.0,<2.0
pyarrow>=14.0            # parquet 읽기/쓰기 엔진. pandas.read_parquet 이 내부적으로 쓴다
numpy>=1.24,<3.0

# --- 통계 (③ 담당) ---
scipy>=1.11,<2.0

# --- 시각화 (② 담당) ---
seaborn>=0.13,<0.14
matplotlib>=3.7,<4.0
plotly>=5.18,<7.0

# --- 머신러닝 (④ 담당) ---
scikit-learn>=1.3,<2.0
joblib>=1.3,<2.0

# --- 테스트 (실행에는 불필요, CI 와 로컬 검증용) ---
pytest>=7.4
pytest-cov>=4.1
